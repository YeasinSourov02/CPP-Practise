// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
#include<algorithm>
int main() {
    string s;
    cin>>s;
    string t;
    cin>>t;
    reverse(s.begin(),s.end());
    if(s==t) cout<<"YES";
    else cout<<"NO";
    return 0;
}
