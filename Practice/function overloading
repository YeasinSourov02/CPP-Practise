#include <iostream>
#include <algorithm>
#include <string>
using namespace std;

string sort(string s) {
    sort(s.begin(), s.end());
    return s;
}

int main() {
    string s = "hello";
    cout << sort(s) << endl; // Output: "ehllo"
    return 0;
}
